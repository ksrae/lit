import { Canvas, Meta, Controls } from '@storybook/blocks';

import * as DesignSystemStories from './design-system.stories';

<Meta of={DesignSystemStories} />

# DesignSystemStories

문서를 만들 수 있습니다.

## Primary
class에 "primary" 를 적용하는 방법.
<Canvas of={DesignSystemStories.Primary} />

## Default
class에 "default" 를 적용하는 방법.
<Canvas of={DesignSystemStories.Default} />

## Parameters
<Controls of={DesignSystemStories.Primary} />